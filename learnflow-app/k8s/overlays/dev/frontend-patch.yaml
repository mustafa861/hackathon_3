apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: learnflow
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: frontend
        env:
        - name: NODE_ENV
          value: "development"
        - name: DEBUG
          value: "true"
